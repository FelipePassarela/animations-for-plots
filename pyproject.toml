[project]
name = "animations-for-plots"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FelipePassarela", email = "126916744+FelipePassarela@users.noreply.github.com" },
]
requires-python = ">=3.13"
dependencies = ["matplotlib>=3.10.6", "numpy>=2.3.3"]

[project.scripts]
pan = "animations_for_plots.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
